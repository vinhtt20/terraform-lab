# Module `web_service` (starter — to be completed)

Đây là khung module. Sau khi hoàn thành các TODO trong `variables.tf`, `main.tf`,
`outputs.tf`, hãy cập nhật bảng inputs/outputs bên dưới (đây cũng là 1 phần
deliverable — RUBRIC cộng điểm cho README module đầy đủ).

## Inputs

| Tên | Kiểu | Default | Mô tả |
|-----|------|---------|-------|
| `name` | `string` | — | TODO |

## Outputs

| Tên | Mô tả |
|-----|-------|
| `container_id` | TODO |

## Ví dụ

```hcl
module "demo" {
  source = "./modules/web_service"
  # TODO: điền các argument
}
```
